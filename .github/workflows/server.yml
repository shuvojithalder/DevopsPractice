name: startup_script
block-style: |
    sudo apt-get update
    sudo apt-get install -y docker.io
    systemctl start docker   
  
fold-style: >
    This script will check the status 
    of the service and then 
    restart it if necessary.




   
